# Impeller V1.1.4 Implementation Plan

1. Add release tests for runtime identity, five-preset instantiation, canonical
   splitter placement, support-profile angle and active-height feasibility.
2. Correct canonical splitter evaluation and inspection placement validation.
3. Adjust the first two preset support profiles and record measured feasibility.
4. Add a compact resolved engineering-drawing model and use it without constructing
   the legacy parameter browser.
5. Replace the active frontend shell with a full-width preset-only review workspace.
6. Remove CFD and feature-debug navigation from the active UI.
7. Run backend/frontend regression, HTTP smoke, and visual browser inspection.
8. Record semantic, insight and verification evidence.
